<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.com.otss.saec_registro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RegistrarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegistrarEntradaActivity"
            android:label="@string/title_activity_registrar_entrada" />
        <activity android:name=".RegistrarSalidaActivity"
            android:label="@string/title_activity_registrar_salida" />
        <activity android:name=".auxiliares.ListadoReporteActivity"
            android:label="Reporte" />
        <activity android:name=".SalirActivity"
            android:label="@string/title_activity_salir" />
        <activity android:name=".ReporteActivity"
            android:label="Reporte"/>
    </application>

</manifest>